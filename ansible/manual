pip3 install docker-compose


ansible-galaxy install yabusygin.gitlab
ansible-galaxy install alexislessard.docker-prometheus
ansible-galaxy install bsmeding.nginx_docker

ansible-playbook -i inventories/servers  playbooks/deploy-gitlab.yml
